import { screenWidth, spacings } from '@open-web-ui-mobile-client-react-native/mobile/shared/ui/styles';

export const chatImagesGroupConfig = {
  maxHeight: 350,
  maxWidth: screenWidth - spacings.contentOffset * 2,
  gap: 4,
};
